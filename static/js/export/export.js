'use strict';
(function(G){
  const U=G.Utils,C=G.Config,R=G.Renderer,S=G.State,E=G.Engine,VP=G.VectorPaths;
  const EX={};

  function renderFrameToCtx(tc,fid,ow,oh){
    tc.fillStyle=S.pc; tc.fillRect(0,0,ow,oh);
    const scale = ow / S.CW; tc.save(); tc.scale(scale, scale);
    const c=S.fc.get(fid);
    if(c && c.P) {
      const tmp = document.createElement('canvas'); tmp.width = S.CW * C.DPR; tmp.height = S.CH * C.DPR;
      const tx = tmp.getContext('2d'); tx.putImageData(c.P, 0, 0); tc.drawImage(tmp, 0, 0, S.CW, S.CH);
    }
    S.layerOrder.forEach(layer => {
      if(c && c[layer]){
        const tmp = document.createElement('canvas'); tmp.width = S.CW * C.DPR; tmp.height = S.CH * C.DPR;
        const tx = tmp.getContext('2d'); tx.putImageData(c[layer], 0, 0); tc.drawImage(tmp, 0, 0, S.CW, S.CH);
      }
      if(VP) VP.renderAllPaths(tc, fid, layer, S.CW, S.CH);
    });
    tc.restore();
  }

EX.exportSVG = () => { 
    const w = S.CW, h = S.CH;
    let svgContent = `<?xml version="1.0" encoding="UTF-8"?>\n<svg xmlns="http://www.w3.org/2000/svg" width="${w}" height="${h}" viewBox="0 0 ${w} ${h}">\n  <title>UgokuDraw Frame ${S.cf+1}</title>\n  <desc>Generated by UgokuDraw v${G.VERSION}</desc>\n  <rect width="100%" height="100%" fill="${S.pc}"/>\n`;
    
    const fid = E.curId();
    E.snapToCache();
    const c = S.fc.get(fid);

    if (c && c.P) {
      const tmp = document.createElement('canvas'); tmp.width = S.CW * C.DPR; tmp.height = S.CH * C.DPR;
      const tx = tmp.getContext('2d'); tx.putImageData(c.P, 0, 0);
      const dataUrl = tmp.toDataURL('image/png');
      svgContent += `  <image href="${dataUrl}" width="${w}" height="${h}" />\n`;
    }

    svgContent += `  <defs>\n`;
    S.layerOrder.forEach(layer => {
      const paths = VP.getFramePaths(fid, layer);
      if(!paths || paths.length === 0) return;
      
      const erasers = paths.filter(p => p.type === 'eraser');
      if (erasers.length > 0) {
        svgContent += `    <mask id="mask-${layer}">\n`;
        svgContent += `      <rect width="100%" height="100%" fill="white" />\n`;
        erasers.forEach(p => {
          const pts = (p.smooth > 0 && p.pts.length >= 3) ? U.stabilize(p.pts, p.smooth) : p.pts;
          if(pts.length === 0) return;
          let d = '';
          if(pts.length === 1) { d = `M ${pts[0].x} ${pts[0].y} L ${pts[0].x+0.1} ${pts[0].y}`; } 
          else {
            d = `M ${pts[0].x} ${pts[0].y}`;
            for(let i=1; i<pts.length-1; i++){
              const mx = (pts[i].x + pts[i+1].x) / 2; const my = (pts[i].y + pts[i+1].y) / 2;
              d += ` Q ${pts[i].x} ${pts[i].y} ${mx} ${my}`;
            }
            d += ` L ${pts[pts.length-1].x} ${pts[pts.length-1].y}`;
          }
          const strokeWidth = p.size; 
          svgContent += `      <path d="${d}" stroke="black" stroke-width="${strokeWidth}" fill="none" stroke-linecap="round" stroke-linejoin="round" opacity="${p.alpha}" />\n`;
        });
        svgContent += `    </mask>\n`;
      }
    });
    svgContent += `  </defs>\n\n`;

    S.layerOrder.forEach(layer => {
      const paths = VP.getFramePaths(fid, layer);
      
      if (!paths || paths.length === 0) {
        if (c && c[layer]) {
          const tmp = document.createElement('canvas'); tmp.width = S.CW * C.DPR; tmp.height = S.CH * C.DPR;
          const tx = tmp.getContext('2d'); tx.putImageData(c[layer], 0, 0);
          const dataUrl = tmp.toDataURL('image/png'); // 逕ｻ蜒丞喧縺励※逵溘▲逋ｽ縺ｫ縺ｪ繧九�繧帝亟縺�
          svgContent += `  <g id="layer-${layer}-raster">\n`;
          svgContent += `    <image href="${dataUrl}" width="${w}" height="${h}" />\n`;
          svgContent += `  </g>\n`;
        }
        return; 
      }
      
      const hasEraser = paths.some(p => p.type === 'eraser');
      const maskAttr = hasEraser ? ` mask="url(#mask-${layer})"` : '';
      
      svgContent += `  <g id="layer-${layer}" opacity="1"${maskAttr}>\n`;
      paths.filter(p => p.type !== 'eraser').forEach(p => {
        const pts = (p.smooth > 0 && p.pts.length >= 3) ? U.stabilize(p.pts, p.smooth) : p.pts;
        if(pts.length === 0) return;
        const color = p.color; const opacity = p.alpha; const strokeWidth = p.size;
        const lineCap = 'round'; const lineJoin = 'round'; let d = ''; let fill = 'none';
        
        if(p.type === 'pen') {
          if(pts.length === 1) { d = `M ${pts[0].x} ${pts[0].y} L ${pts[0].x+0.1} ${pts[0].y}`; } 
          else {
            d = `M ${pts[0].x} ${pts[0].y}`;
            for(let i=1; i<pts.length-1; i++){
              const mx = (pts[i].x + pts[i+1].x) / 2; const my = (pts[i].y + pts[i+1].y) / 2;
              d += ` Q ${pts[i].x} ${pts[i].y} ${mx} ${my}`;
            }
            d += ` L ${pts[pts.length-1].x} ${pts[pts.length-1].y}`;
          }
        } else if (p.type === 'line') { d = `M ${pts[0].x} ${pts[0].y} L ${pts[pts.length-1].x} ${pts[pts.length-1].y}`;
        } else if (p.type === 'rect') { const w = pts[1].x - pts[0].x; const h = pts[1].y - pts[0].y; d = `M ${pts[0].x} ${pts[0].y} h ${w} v ${h} h ${-w} z`; if(p.fill) fill = color;
        } else if (p.type === 'circle') { const rx = Math.abs(pts[1].x - pts[0].x)/2; const ry = Math.abs(pts[1].y - pts[0].y)/2; const cx = (pts[0].x + pts[1].x)/2; const cy = (pts[0].y + pts[1].y)/2; d = `M ${cx+rx} ${cy} a ${rx} ${ry} 0 1 0 ${-2*rx} 0 a ${rx} ${ry} 0 1 0 ${2*rx} 0`; if(p.fill) fill = color;
        } else if (p.type === 'star' || p.type === 'heart') { d = `M ${pts[0].x} ${pts[0].y}`; for(let i=1; i<pts.length; i++) d += ` L ${pts[i].x} ${pts[i].y}`; if(p.type==='rect'||p.type==='circle'||p.type==='star'||p.type==='heart') d+=' Z'; if(p.fill) fill = color; }

        if(p.outline) { const outWidth = strokeWidth + p.outlineWidth * 2; svgContent += `    <path d="${d}" stroke="${p.outlineColor}" stroke-width="${outWidth}" fill="${fill}" stroke-linecap="${lineCap}" stroke-linejoin="${lineJoin}" opacity="${opacity}" />\n`; }
        svgContent += `    <path d="${d}" stroke="${color}" stroke-width="${strokeWidth}" fill="${fill}" stroke-linecap="${lineCap}" stroke-linejoin="${lineJoin}" opacity="${opacity}" />\n`;
      });
      svgContent += `  </g>\n`;
    });
    
    svgContent += `</svg>`;
    downloadBlob(new Blob([svgContent], {type: 'image/svg+xml;charset=utf-8'}), `ugoku_frame_${S.cf+1}.svg`);
    E.toast('繝吶け繧ｿ繝ｼSVG菫晏ｭ伜ｮ御ｺ�');
  };

  EX.exportGIF=async quality=>{E.snapToCache();for(let i=0;i<S.frames.length;i++)await E.ensureCached(S.frames[i].id);const mo=U.$('exportModal');mo.classList.add('show');U.$('expTitle').textContent='GIF逕滓�荳ｭ...';const sc=quality==='low'?0.5:quality==='mid'?0.75:1;const ow=Math.round(S.CW*sc),oh=Math.round(S.CH*sc);const delay=Math.round(1000/S.fps);const tmp=document.createElement('canvas');tmp.width=ow;tmp.height=oh;const tc=tmp.getContext('2d');const framesData=[];for(let i=0;i<S.frames.length;i++){renderFrameToCtx(tc,S.frames[i].id,ow,oh);framesData.push(tc.getImageData(0,0,ow,oh).data.buffer);U.$('expBar').style.width=((i+1)/S.frames.length*40)+'%';U.$('expMsg').textContent=`繝ｬ繝ｳ繝繝ｪ繝ｳ繧ｰ ${i+1}/${S.frames.length}`;await new Promise(r=>setTimeout(r,2));}if(E.W){const res=await E.workerCall({type:'gifEncode',w:ow,h:oh,framesData,delay},framesData,d=>{U.$('expBar').style.width=(40+d.frame/d.total*55)+'%';U.$('expMsg').textContent=`繧ｨ繝ｳ繧ｳ繝ｼ繝� ${d.frame}/${d.total}`;});U.$('expBar').style.width='100%';downloadBlob(new Blob([new Uint8Array(res.data)],{type:'image/gif'}),'ugoku_draw_'+Date.now()+'.gif');mo.classList.remove('show');E.toast('GIF菫晏ｭ伜ｮ御ｺ�');}else{mo.classList.remove('show');E.toast('Worker髱槫ｯｾ蠢�');}};
  EX.exportPNGSequence=async quality=>{E.snapToCache();for(let i=0;i<S.frames.length;i++)await E.ensureCached(S.frames[i].id);const sc=quality==='low'?0.5:quality==='mid'?1:2;const ow=Math.round(S.CW*sc),oh=Math.round(S.CH*sc);const tmp=document.createElement('canvas');tmp.width=ow;tmp.height=oh;const tc=tmp.getContext('2d');const mo=U.$('exportModal');mo.classList.add('show');U.$('expTitle').textContent='PNG蜃ｺ蜉帑ｸｭ...';for(let i=0;i<S.frames.length;i++){renderFrameToCtx(tc,S.frames[i].id,ow,oh);const blob=await new Promise(r=>tmp.toBlob(r,'image/png'));downloadBlob(blob,`frame_${String(i+1).padStart(4,'0')}.png`);U.$('expBar').style.width=((i+1)/S.frames.length*100)+'%';U.$('expMsg').textContent=`${i+1}/${S.frames.length}`;await new Promise(r=>setTimeout(r,50));}mo.classList.remove('show');E.toast('PNG騾｣逡ｪ蜃ｺ蜉帛ｮ御ｺ�');};
  EX.exportCurrentFrame=()=>{const tmp=document.createElement('canvas');tmp.width=S.CW;tmp.height=S.CH;const tc=tmp.getContext('2d');E.snapToCache();renderFrameToCtx(tc,E.curId(),S.CW,S.CH);tmp.toBlob(blob=>{downloadBlob(blob,`ugoku_frame_${S.cf+1}.png`);E.toast('繝輔Ξ繝ｼ繝�逕ｻ蜒丈ｿ晏ｭ�');},'image/png');};

  async function renderPerfectAudioBuffer() {
    const duration = Math.max(0.1, S.frames.length / S.fps);
    const sampleRate = 44100;
    const oac = new OfflineAudioContext(2, Math.floor(sampleRate * duration), sampleRate);
    let hasAudio = false;
    if (G.Audio && G.Audio.bgmBuffer) {
      const bgmNode = oac.createBufferSource(); bgmNode.buffer = G.Audio.bgmBuffer;
      bgmNode.connect(oac.destination); bgmNode.start(0); hasAudio = true;
    }
    S.frames.forEach((fr, i) => {
      const time = i / S.fps;
      if (fr.seFlags) {
        fr.seFlags.forEach((flag, chIdx) => {
          if (flag && S.seBuffers && S.seBuffers[chIdx]) {
            const seNode = oac.createBufferSource(); seNode.buffer = S.seBuffers[chIdx];
            seNode.connect(oac.destination); seNode.start(time); hasAudio = true;
          }
        });
      }
    });
    if (!hasAudio) return null;
    return await oac.startRendering();
  }

  function getPlanarData(buffer, offset, frameCount) {
    const channels = buffer.numberOfChannels;
    const data = new Float32Array(channels * frameCount);
    for (let c = 0; c < channels; c++) {
      const channelData = buffer.getChannelData(c);
      data.set(channelData.subarray(offset, offset + frameCount), c * frameCount);
    }
    return data;
  }

  EX.exportVideo = async quality => {
    if (typeof window.VideoEncoder === 'undefined' || typeof window.Mp4Muxer === 'undefined') {
      return E.toast('縺泌茜逕ｨ縺ｮ繝悶Λ繧ｦ繧ｶ縺ｯMP4蜃ｺ蜉帶ｩ溯�縺ｫ髱槫ｯｾ蠢懊〒縺�');
    }

    E.snapToCache();
    for(let i=0; i<S.frames.length; i++) await E.ensureCached(S.frames[i].id);
    
    const mo = U.$('exportModal'); mo.classList.add('show');
    U.$('expTitle').textContent = 'MP4蜍慕判繧偵お繝ｳ繧ｳ繝ｼ繝我ｸｭ...';
    U.$('expBar').style.width = '0%';
    U.$('expMsg').textContent = '貅門ｙ荳ｭ';

    try {
      const audioBuffer = await renderPerfectAudioBuffer();
      
      // 逕ｻ雉ｪ險ｭ螳壹→繝薙ャ繝医Ξ繝ｼ繝�
      const sc = quality==='low' ? 0.5 : quality==='mid' ? 1 : 1.5;
      const bps = quality==='low' ? 2e6 : quality==='mid' ? 5e6 : 10e6;
      
      // 繧ｭ繝｣繝ｳ繝舌せ繧ｵ繧､繧ｺ�医ヵ繧ｩ繝ｼ繝ｫ繝舌ャ繧ｯ莉倥″繝ｻ遒ｺ螳溘↓蛛ｶ謨ｰ縺ｫ縺吶ｋ��
      const baseW = Number(S.CW) || 800;
      const baseH = Number(S.CH) || 800;
      let ew = Math.floor(baseW * sc);
      let eh = Math.floor(baseH * sc);
      ew = (ew % 2 === 0) ? ew : ew + 1;
      eh = (eh % 2 === 0) ? eh : eh + 1;
      if (ew < 2) ew = 2;
      if (eh < 2) eh = 2;

      const ec = document.createElement('canvas'); ec.width = ew; ec.height = eh;
      const ex = ec.getContext('2d');

      const muxer = new Mp4Muxer.Muxer({
        target: new Mp4Muxer.ArrayBufferTarget(),
        video: { codec: 'avc', width: ew, height: eh },
        audio: audioBuffer ? { codec: 'aac', sampleRate: 44100, numberOfChannels: 2 } : undefined,
        fastStart: 'in-memory'
      });

      const videoEncoder = new VideoEncoder({
        output: (chunk, meta) => muxer.addVideoChunk(chunk, meta),
        error: e => console.error(e)
      });
      
      // 笘� 縺薙％縺ｫ width 縺ｨ height 繧定ｿｽ蜉�縺励∪縺励◆��
      videoEncoder.configure({
        codec: 'avc1.42E01F', 
        width: ew,
        height: eh,
        bitrate: bps,
        framerate: S.fps
      });

      let audioEncoder = null;
      if (audioBuffer) {
        audioEncoder = new AudioEncoder({
          output: (chunk, meta) => muxer.addAudioChunk(chunk, meta),
          error: e => console.error(e)
        });
        audioEncoder.configure({
          codec: 'mp4a.40.2', // AAC
          sampleRate: 44100,
          numberOfChannels: 2,
          bitrate: 128000
        });
      }

      for (let i = 0; i < S.frames.length; i++) {
        EX.renderFrameToCtx(ex, S.frames[i].id, ew, eh);
        
        const timestamp = (i / S.fps) * 1000000;
        const frame = new VideoFrame(ec, { timestamp });
        
        const keyFrame = (i % (S.fps * 2)) === 0;
        videoEncoder.encode(frame, { keyFrame });
        frame.close(); 

        U.$('expBar').style.width = ((i+1) / S.frames.length * 50) + '%';
        U.$('expMsg').textContent = `譏�蜒上お繝ｳ繧ｳ繝ｼ繝�: ${i+1}/${S.frames.length}`;
        await new Promise(r => setTimeout(r, 0)); // 繝悶Λ繧ｦ繧ｶ繧偵ヵ繝ｪ繝ｼ繧ｺ縺輔○縺ｪ縺�◆繧√�鬲疲ｳ�
      }

      if (audioBuffer && audioEncoder) {
        const totalFrames = audioBuffer.length;
        const framesPerChunk = 1024; 
        
        for (let offset = 0; offset < totalFrames; offset += framesPerChunk) {
          const frameCount = Math.min(framesPerChunk, totalFrames - offset);
          const audioData = new AudioData({
            format: 'f32-planar',
            sampleRate: 44100,
            numberOfChannels: 2,
            numberOfFrames: frameCount,
            timestamp: (offset / 44100) * 1000000,
            data: getPlanarData(audioBuffer, offset, frameCount)
          });
          audioEncoder.encode(audioData);
          audioData.close();
          
          U.$('expBar').style.width = (50 + (offset / totalFrames * 50)) + '%';
          U.$('expMsg').textContent = '髻ｳ螢ｰ繧ｨ繝ｳ繧ｳ繝ｼ繝我ｸｭ...';
          if (offset % (framesPerChunk * 10) === 0) await new Promise(r => setTimeout(r, 0));
        }
      }

      U.$('expMsg').textContent = '譛邨ょ�逅�ｼ育ｮｱ隧ｰ繧�ｼ峨ｒ陦後▲縺ｦ縺�∪縺�...';

      await videoEncoder.flush();
      if (audioEncoder) await audioEncoder.flush();

      muxer.finalize();
      const { buffer } = muxer.target;
      const blob = new Blob([buffer], { type: 'video/mp4' });
      downloadBlob(blob, `ugoku_draw_${Date.now()}.mp4`);

      E.toast('MP4蜃ｺ蜉� 螳御ｺ�ｼ�');
      mo.classList.remove('show');

    } catch (err) {
      console.error(err);
      E.toast('繧ｨ繝ｳ繧ｳ繝ｼ繝我ｸｭ縺ｫ繧ｨ繝ｩ繝ｼ縺檎匱逕溘＠縺ｾ縺励◆');
      mo.classList.remove('show');
    }
  };

  function downloadBlob(blob,fn){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=fn;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),5000);}
  EX.renderFrameToCtx=renderFrameToCtx; EX.downloadBlob=downloadBlob;
  G.Export=EX;
})(window.UgokuDraw);
